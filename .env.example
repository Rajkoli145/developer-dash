# Workspace owner — created automatically on first launch
OWNER_EMAIL=owner@localhost
OWNER_NAME=

# Database (SQLite for development; switch to postgres:// for production)
DATABASE_URL="file:./dev.db"

# Where uploaded document files are stored (relative to the project root)
UPLOAD_DIR="uploads"
